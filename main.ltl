// ltl-chess-perft/main.ltl
//
// Chess move generator verified via Perft positions.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-chess-perft v0.1.0")
    io::println("Chess move generator verified via Perft positions.")
    0
}
